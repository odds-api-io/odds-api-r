% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/endpoints.R
\name{oa_events}
\alias{oa_events}
\title{List events for a sport}
\usage{
oa_events(
  sport,
  league = NULL,
  status = NULL,
  from = NULL,
  to = NULL,
  limit = NULL,
  skip = NULL,
  bookmaker = NULL,
  participant_id = NULL,
  api_key = NULL
)
}
\arguments{
\item{sport}{Sport slug, e.g. \code{"football"}. See \code{\link[=oa_sports]{oa_sports()}}.}

\item{league}{Optional league slug, e.g. \code{"england-premier-league"}.}

\item{status}{Optional statuses to include: any of \code{"pending"}, \code{"live"},
\code{"settled"}. A vector is collapsed to a comma-separated list.}

\item{from, to}{Optional RFC 3339 date-times bounding the event window,
e.g. \code{"2026-09-01T00:00:00Z"}.}

\item{limit}{Optional maximum number of events (capped at 5000).}

\item{skip}{Optional pagination offset, used together with \code{limit}.}

\item{bookmaker}{Optional bookmaker name; only events with odds from that
bookmaker are returned.}

\item{participant_id}{Optional team or participant id to filter by.}

\item{api_key}{API key. Defaults to the \code{ODDS_API_KEY} environment
variable. Not required for this endpoint.}
}
\value{
A data frame with one row per event.
}
\description{
Returns upcoming, live or settled events. When \code{to} is omitted the API
returns events within the next 14 days, hard-capped at 5000 rows per
response; use \code{limit} and \code{skip} to paginate.
}
\examples{
\dontrun{
events <- oa_events("football", league = "england-premier-league")
live <- oa_events("football", status = c("pending", "live"))
}
}
