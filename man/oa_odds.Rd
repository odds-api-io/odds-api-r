% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/endpoints.R
\name{oa_odds}
\alias{oa_odds}
\title{Get odds for an event}
\usage{
oa_odds(event_id, bookmakers, api_key = NULL)
}
\arguments{
\item{event_id}{Event id, from \code{\link[=oa_events]{oa_events()}}.}

\item{bookmakers}{Character vector of bookmaker names (max 30), e.g.
\code{c("Bet365", "SingBet")}. Required by the API.}

\item{api_key}{API key. Defaults to the \code{ODDS_API_KEY} environment
variable. Not required for this endpoint.}
}
\value{
A data frame with one row per odds line, with columns \code{eventId},
\code{bookmaker}, \code{market}, \code{updatedAt} and the price columns present in the
response (such as \code{home}, \code{draw}, \code{away}, \code{hdp}, \code{over}, \code{under}).
}
\description{
Fetches odds for one event from up to 30 bookmakers and returns them as a
tidy data frame with one row per price line.
}
\examples{
\dontrun{
odds <- oa_odds(123456, bookmakers = c("Bet365", "SingBet"))
subset(odds, market == "ML")
}
}
